<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="com.monkeyking.permission.PLUGIN" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:extractNativeLibs="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <meta-data
            android:name="com.monkeyking.plugin.WAKE_ACTIVITY"
            android:value=".WakeActivity" />

        <meta-data
            android:name="com.monkeyking.plugin.info.AUTHOR"
            android:value="SuperMonster003" />

        <activity
            android:name=".WakeActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="true"
            android:permission="com.monkeyking.permission.PLUGIN"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
                <action android:name="com.monkeyking.plugin.action.WAKE" />
            </intent-filter>
        </activity>

        <service
            android:name=".PpOcrV5PluginService"
            android:exported="true"
            android:permission="com.monkeyking.permission.PLUGIN">
            <intent-filter>
                <action android:name="com.monkeyking.plugin.PADDLE_OCR" />
                <category android:name="paddle-ocr" />
            </intent-filter>
        </service>

    </application>

</manifest>
